import { PROJECTS, sandbox, startServer, tool, validId } from '../../lib/core.mjs';import { detectProjectStack, inspectHost, inspectTools, setupPlan } from '../../lib/environment.mjs';
const project=a=>sandbox(PROJECTS,validId(a.project_id,'project_id'));
const props={project_id:{type:'string'}};
const tools=[tool('environment.inspect','Inspects host and installed development tools without changing the system.',['environment.read'],{properties:{tools:{type:'array'}}},async a=>({host:inspectHost(),tools:await inspectTools(a.tools)})),tool('environment.detect_stack','Detects project stack from manifests and lockfiles.',['environment.read'],{required:['project_id'],properties:props},async a=>detectProjectStack(project(a))),tool('environment.plan_setup','Builds a setup plan from observed host, stack and tools.',['environment.read'],{required:['project_id'],properties:props},async a=>{const host=inspectHost(),stack=await detectProjectStack(project(a)),installed=await inspectTools();return setupPlan(host,stack,installed)})];startServer({name:'uplex-environment-mcp',version:'1.0.0'},tools);
