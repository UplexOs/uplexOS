import { startServer, tool, validId } from '../../lib/core.mjs';
import { consumeApproval, decideApproval, requestApproval } from '../../../.uplex/runtime/governance.mjs';
const tools=[
 tool('approval.request','Registra solicitação de aprovação.',['approval.write'],{required:['project_id','action','risk_summary'],properties:{project_id:{type:'string'},action:{type:'string'},risk_summary:{type:'string'},ttl_minutes:{type:'number'}}},async(a,c)=>{validId(a.project_id,'project_id');return requestApproval({projectId:a.project_id,action:a.action,riskSummary:a.risk_summary,ttlMinutes:a.ttl_minutes,actor:c.actor??'unknown'})},{readOnly:false,risk:'medium'}),
 tool('approval.decide','Aprova ou rejeita solicitação.',['approval.write'],{required:['approval_id','decision'],properties:{approval_id:{type:'string'},decision:{enum:['approved','rejected']}}},async(a,c)=>decideApproval({approvalId:a.approval_id,decision:a.decision,actor:c.actor??'unknown'}),{readOnly:false,risk:'high'}),
 tool('approval.consume','Consome aprovação válida de uso único.',['approval.consume'],{required:['approval_id','action','project_id'],properties:{approval_id:{type:'string'},action:{type:'string'},project_id:{type:'string'}}},async a=>consumeApproval({approvalId:a.approval_id,action:a.action,projectId:a.project_id}),{readOnly:false,risk:'high'})
];startServer({name:'uplex-approval-mcp',version:'1.0.0'},tools);
